using UnityEngine;
using Game.Events.Channel;
namespace Game.Collectibles
{
    public class IceCubeController{

/*
        IceCubeModel          -> Stores data at runtime
        IceCubeView           -> Broadcasts it into the channel
        iceCubeService        -> Stores/loads the data on disk

        IceCubeSpawnManager   -> Takes a set of positions and set of entities 
                                 and spawns them based on chosen strategy.
                                 spawner invokes the strategy and passes the 
                                 position to factory, who simply instantiates
*/

    }
}
